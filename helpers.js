// function sum(a, b) {
//     return a + b;
// }
/**
 * instead of using simple format of function
 * we can use modern js's Arrow function
 * that is look like below
 */
// const sum = (a, b) => {
//     return a + b;
// };
/**
 *you make this more simple if function body only has one line
 * with return statement
 */
// const sum = (a, b) => a + b;
// module.exports = {
//     sum
// };
/**
 * you can make this whole process more simpler using
 */
exports.sum = (a, b) => a + b;